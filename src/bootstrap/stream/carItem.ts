export interface Car {
    id: number;
    name: string; // done
    description: string; // done
    brand: string; // done
    year: number; // done
    price: number; // done
    travelled: number;
    mileage: number;
    owner: number;
    image: string;
    created_at: string;
    updated_at: string;
}