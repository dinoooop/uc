export interface User {
    id?: number;
    first_name: string; // done
    email: string; // done
    password?: string; // done
    phone?: string; // done
    about: string; // done
    avatar: string;
}